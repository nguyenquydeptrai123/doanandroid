<?xml version="1.0" encoding="utf-8"?>

<androidx.cardview.widget.CardView style="@style/cardView"
    android:layout_margin="5dp"
    app:cardCornerRadius="4dp"
    app:cardElevation="4dp"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <LinearLayout
        style="@style/linearDefault"
        android:padding="10dp"
        android:orientation="horizontal">

        <ImageView
           android:layout_width="50dp"
            android:layout_height="50dp"
            android:scaleType="fitXY"
            android:layout_marginRight="20dp"
            android:id="@+id/main_kieuLoai" />

        <LinearLayout style="@style/linearDefault"
            android:orientation="vertical">

            <TextView
                android:id="@+id/main_moTa"
                android:text="TEST"
                style="@style/textViewCardView" />


            <TextView
                android:id="@+id/main_soLuong"
                android:text="TEST"
                style="@style/textViewCardView" />
        </LinearLayout>

    </LinearLayout>


</androidx.cardview.widget.CardView>